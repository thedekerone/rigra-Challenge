type Product {
	title: String
	id: Int
	price: Float
	img: String
}

type Query {
	getProducts: [Product]
}

type Mutation {
	createProduct(input: ProductInput): Product
}

# inputs

input ProductInput {
	title: String!
	price: Float!
	img: String!
	quantity: Int
}
